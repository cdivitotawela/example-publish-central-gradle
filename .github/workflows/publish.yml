name: Publish

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: gradle:8.1
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Assemble
        run: ./gradlew assemble

